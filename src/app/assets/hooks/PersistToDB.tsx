"use client"

import React from "react";


export default function PersistToDB({ data } : { data: { name: string; payload: any } }){ 

    if(data.name === "cart"){

    }

    React.useEffect(() => {
    }, [])

    return null;
}   
